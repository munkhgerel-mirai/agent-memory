{
  "name": "agent-memory",
  "version": "0.1.0",
  "description": "AI-DLC-native lifecycle memory for agentic development workflows.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node dist/tests/lifecycle-memory-core.test.js && node dist/tests/local-workspace-storage.test.js && node dist/tests/retrieval-context.test.js && node dist/tests/privacy-governance.test.js && node dist/tests/framework-agnostic-integration.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}
