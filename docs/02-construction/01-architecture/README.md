# Architecture

High-level system architecture, context diagrams, architecture decisions, and data flow.

Create or update architecture artifacts during Logical Design when cross-Unit context, component boundaries, data flows, or architecture decisions need to be captured.

Use `brownfield_discovery_TEMPLATE.md` when documenting an existing codebase before design or implementation changes.

Use `technology_decisions_TEMPLATE.md` when documenting approval-gated technology decisions and ADR entries.
